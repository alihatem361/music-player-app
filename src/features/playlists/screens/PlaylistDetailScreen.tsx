import React from 'react';
import { EmptyState, ScreenContainer } from '../../../components';

/** Placeholder shell — replaced when this feature is implemented. */
export const PlaylistDetailScreen: React.FC = () => (
  <ScreenContainer>
    <EmptyState title="Playlist" message="Playlist tracks show up here." />
  </ScreenContainer>
);
